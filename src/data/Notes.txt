Add a variable that is the strength of the evidence: randomized trial 
vs observational study

Insert the actual costs per 1k tokens for DeepSeek and Qwen in def _set_cost_rates()

Go through about 100 examples to create a dataset of training examples.

quality assurance workflow: first model extracts → validation catches issues 
→ second model verifies problematic extractions → humans review what both 
models couldn't handle.

I need a bank card for Qwen and deepseek
Will experience with free alternatives
Grok or Ollama

Works but need to debug. JSON parsing errors.