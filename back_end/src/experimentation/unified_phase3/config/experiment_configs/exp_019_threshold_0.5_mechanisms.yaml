# Experiment 019: Distance Threshold 0.5 - Mechanisms
# =====================================================

_base: "../base_config.yaml"

experiment:
  name: "threshold_0.5_mechanisms"
  description: "Test distance_threshold=0.5 for mechanism clustering (current default baseline)"
  tags:
    - threshold_experiment
    - hierarchical
    - mechanisms
    - threshold_0.5
    - baseline

clustering:
  # Keep interventions/conditions as HDBSCAN (not testing)
  interventions:
    algorithm: "hdbscan"
    min_cluster_size: 2
    min_samples: 1
    cluster_selection_epsilon: 0.0
    metric: "euclidean"

  conditions:
    algorithm: "hdbscan"
    min_cluster_size: 2
    min_samples: 1
    cluster_selection_epsilon: 0.0
    metric: "euclidean"

  mechanisms:
    algorithm: "hierarchical"
    linkage: "ward"
    distance_threshold: 0.5
    n_clusters: null
    metric: "euclidean"

naming:
  llm:
    temperature: 0.0
