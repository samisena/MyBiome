# Experiment 023: Distance Threshold 0.8 - Conditions
# =====================================================

_base: "../base_config.yaml"

experiment:
  name: "threshold_0.8_conditions"
  description: "Test distance_threshold=0.8 for condition clustering (looser grouping)"
  tags:
    - threshold_experiment
    - hierarchical
    - conditions
    - threshold_0.8

clustering:
  # Keep interventions as HDBSCAN (not testing)
  interventions:
    algorithm: "hdbscan"
    min_cluster_size: 2
    min_samples: 1
    cluster_selection_epsilon: 0.0
    metric: "euclidean"

  conditions:
    algorithm: "hierarchical"
    linkage: "ward"
    distance_threshold: 0.8
    n_clusters: null
    metric: "euclidean"

  mechanisms:
    algorithm: "hdbscan"
    min_cluster_size: 2
    min_samples: 1
    cluster_selection_epsilon: 0.0
    metric: "euclidean"

naming:
  llm:
    temperature: 0.0
